mixin article(title)
  .article
    .article-wrapper
      h1= title
      if block
        block
      else
        p No content provided

// 不使用mixin块
+article('Hello world')

// 使用mixin块
+article('Hello world')
  p This is my
  p Amazing article